apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: podinfo-httpproxy
  namespace: default
spec:
  virtualhost:
    fqdn: contour.local
  routes:
    - conditions:
      - prefix: /podinfo
      services:
        - name: podinfo
          port: 9898                                 
